package fr.dr.gfirefoxdata.parser

import groovy.json.JsonSlurper

/**
 * JsonParser.
 * User: drieu
 * Date: 01/05/14
 */
class JsonParser {

    def getSearch(def path) {
// def inputFile = new File(path)
// def InputJSON = new JsonSlurper().parseText(inputFile.text)
// InputJSON.each{ println it }
    }
}